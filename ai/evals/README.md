# AI Evals

This crate runs lightweight evaluation checks against JSONL fixtures.

## Run
- From repo root: `cargo run -p ai_evals`

## Add cases
- Add JSONL rows in `fixtures/sample_cases.jsonl`.
- Update `evals.toml` to define thresholds or metadata.
